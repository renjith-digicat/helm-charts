apiVersion: v1
kind: Service
metadata:
  name: {{ include "prediction-server-chart.fullname" . }}
  labels:
    {{- include "prediction-server-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "prediction-server-chart.selectorLabels" . | nindent 4 }}
